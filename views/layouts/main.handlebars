<hmtl>
	<head>
		<title>
			Asynxy: {{page_name}}
		</title>
		<link href='https://fonts.googleapis.com/css?family=Oswald' rel='stylesheet'>
		<style>
			body {
				font-family: 'Oswald';
				font-size: 24px;
				background-color: #3C4556;
			}

			#login_form, #command {
				text-align: center;
			}

			#login_form form:hover {
				box-shadow: 0px 3px 3px 3px black;
				transition: 0.1s;
			}

		</style>
		<script type="text/javascript">
			function sleep(ms) {
      			return new Promise(resolve => setTimeout(resolve, ms));
   			}

			function slavename_change(master_name){
				var url = "http://localhost:3000/ping";
				var params = "master="+master_name;
				var http = new XMLHttpRequest();
				http.open("GET", url+"?"+params, true);
				http.onreadystatechange = function(){
				    if(http.readyState == 4 && http.status == 200) {
				        var element = document.getElementById("slavenames");
						element.innerHTML = http.responseText;
						sleep(2000).then(() => {
							slavename_change(master_name);
						});
				    }
				}
				http.send(null);
			}
		</script>
	</head>
	<body>
		{{{body}}}
		<div style="text-align:center; color:#F8B32F;">
			{{#if error}} 
				<div style="margin-bottom: 10px">{{error}}</div>
				<form method='POST' action="/create_and_log">
					<input type="hidden" name="master" value="{{master}}">
					<input type="hidden" name="master_pass" value="{{pass}}">
					<input type="submit" value="Accept" style="outline: none; border: none; border-radius: 10px; padding-left: 15px; padding-right: 15px; padding-bottom: 7px; padding-top: 7px; background: white">
				</form>
			{{/if}}
			{{#if error2}} 
				<p align="center" style="color:#E7503B">{{error2}}</p>
			{{/if}}
		</div>
	</body>
</hmtl>
